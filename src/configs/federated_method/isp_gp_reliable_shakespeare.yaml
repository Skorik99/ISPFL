defaults:
  - isp_gp
  - _self_

_target_: federated_methods.isp_gp.ISPGPShakespeare
num_clients_subset: 100
audit_interval: 20

support:
  pool_size: 100
  grid: [20, 30, 40, 50, 60, 70, 80, 90, 100]

proxy_cohort:
  bias_parameter: output.bias

evaluator:
  tensor_cache: disabled
